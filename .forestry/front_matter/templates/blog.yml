---
label: Blog
hide_body: false
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
  description: The page title.
- name: date
  type: datetime
  label: Date
  description: The date you want to display on the blog post.
  config:
    required: false
    date_format: DD/MM/YYYY
    time_format: 
    display_utc: false
    export_format: YYYY-MM-DD
  default: now
- name: layout
  type: select
  default: layouts/post.html
  config:
    required: true
    options:
    - layouts/post.html
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Layout
  hidden: true
- name: featuredImage
  type: file
  config:
    maxSize: 64
  label: Featured Image
- name: excerpt
  type: textarea
  default: ''
  config:
    required: true
    wysiwyg: false
    schema:
      format: markdown
  label: Excerpt
pages:
- src/blog/attending-my-first-orchestra-visiting-the-british-museum-and-finishing-the-licanius-trilogy.md
